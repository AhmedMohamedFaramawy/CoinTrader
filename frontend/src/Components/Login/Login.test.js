import {render , fireEvent} from "@testing-library/react" ;
import Login from "./Login";


/*
it("Login with Google",  () => {
    const { queryByTitle } =  render(<Login />);
    const input = queryByTitle("GoogleLogin");
})
*/